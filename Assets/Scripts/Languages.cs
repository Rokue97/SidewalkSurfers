using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Languages : MonoBehaviour
{
    [SerializeField]
    Text gameScoreText, menuScoreText, highText, leaderboardtext, meText, shopText, meSectionTitle, shopTitle, shopCoinText, shopGemText, leaderboardTitle,
        gameoverTitle, saveMeText, skipText, afterGameoverTitle, afterGameoverScoretext, afterGameoverMainmenu, afterGameoverPlayAgain, infoTitle, infoOKButtonText,
        logInText, timerText, settingsTitle, settingsMusicText, musicOnTexti, musicOffText, settingsSFXText, sfxOnText, sfxOffText, volumeText, settingsMainMenu,
        settingsResumeText, settingsCredits, settingsSignIn, settingsSignOut;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void English()
    {

    }
    
}
